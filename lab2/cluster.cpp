#include "cluster.h"
#include <tuple>
using namespace std;
//удачи <3
// 0(*^*)0 (OxO) (OwO) (OmO) (OoO) (XoX) ┬─┬ ノ( ゜-゜ノ)  ¯\_(ツ)_/¯ (╯°□°）╯︵ ┻━┻
class Algorithm {
public:
	void m1_algorithm(pair<size_t, size_t> matrix_1, pair<size_t, size_t> matrix_2)
	{
		int work_ticks = 0;
		for (int ticks_count = 0;; ticks_count++)
		{
			
		}
		
	}
	void m2_algorithm();
};